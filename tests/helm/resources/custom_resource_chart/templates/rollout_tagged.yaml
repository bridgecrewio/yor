apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  labels:
    yor_name: {{ .Release.Name }}-rollout
    yor_trace: "00000000-0000-0000-0000-000000000001"
  name: {{ .Release.Name }}-rollout
spec:
  replicas: 2
  strategy:
    canary:
      steps:
        - setWeight: 20
